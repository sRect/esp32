#pragma once

// Copy this file to mqtt_secrets.h and replace the placeholder locally.
// mqtt_secrets.h is ignored by Git and must never be committed.
#define MQTT_PASSWORD "replace-with-device-mqtt-password"
