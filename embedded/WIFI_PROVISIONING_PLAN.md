# ESP32-S3 Wi-Fi 配网与 Android App 开发计划

## 1. 项目目标

为 ESP32-S3-N16R8 开发 Wi-Fi 配网固件，同时开发配套 Android App。

本文档暂定采用 **ESP32 SoftAP 配网方案**：ESP32 创建临时 Wi-Fi 热点，Android App 连接该热点，通过局域网接口将家庭 Wi-Fi 的 SSID 和密码发送给 ESP32。

> ESP32-S3 仅支持 2.4GHz Wi-Fi，配网流程需要明确提示用户选择 2.4GHz 网络。

## 2. 产品需求确认

开发前需要确定以下内容：

- 配网方式：ESP32 SoftAP 配网。
- Android 最低支持版本。
- ESP32 热点名称规则：`esp32-xxx`，`xxx` 为设备 ID 最后3位的小写形式。
- ESP32 临时热点为开放网络，不设置密码；修改接口仍要求随机 token。
- 是否允许用户重新配网。
- 如何清除旧的 Wi-Fi 配置。
- 配网成功后是否需要连接后端服务器。
- 是否需要将设备绑定到用户账号。
- 是否考虑多台设备同时配网。
- Wi-Fi 密码等敏感数据的安全要求。
- 是否用于量产或仅用于内部开发测试。

## 3. 整体配网流程

1. ESP32 首次启动时检查本地是否保存了 Wi-Fi 信息。
2. 用户长按 BOOT 键5秒，ESP32 进入配网模式，RGB 指示灯开始蓝色呼吸。
3. ESP32 创建临时热点，例如 `esp32-c9c`。
4. Android App 扫描或引导用户选择该热点。
5. Android App 请求系统连接 ESP32 热点。
6. App 连接成功后，访问 ESP32 的本地服务。
7. App 请求 ESP32 扫描附近的 2.4GHz Wi-Fi。
8. 用户选择家庭 Wi-Fi，并输入密码。
9. App 将 SSID 和密码发送给 ESP32。
10. ESP32 保存配置信息，并尝试连接目标路由器。
11. ESP32 将连接进度和结果返回 App。
12. 连接成功后，ESP32 关闭临时热点并进入正常工作模式。
13. 连接失败时，ESP32 返回具体失败原因，App 允许用户重新操作。

## 4. 通信协议设计

建议 ESP32 在配网热点中启动 HTTP 服务，默认地址可使用：

```text
http://192.168.4.1
```

### 4.1 建议接口

#### 获取设备信息

```http
GET /api/v1/device
```

返回内容包括：

- 设备型号
- 设备唯一 ID
- 固件版本
- 配网状态
- 是否曾经成功配过网
- Wi-Fi MAC 地址

建议返回示例：

```json
{
  "deviceModel": "ESP32-S3-N16R8",
  "deviceId": "C78E1B1BCFC9",
  "firmwareVersion": "1.0.0",
  "provisioningState": "awaiting_config",
  "hasProvisionedBefore": true,
  "wifiMac": "7C:E8:B1:B1:FC:9C",
  "apSsid": "esp32-c9c",
  "provisioningToken": "本次启动生成的随机令牌",
  "apIp": "192.168.4.1"
}
```

字段说明：

- `hasProvisionedBefore` 表示设备历史上是否至少成功完成过一次配网。
- 该字段应持久化保存到 NVS，设备重启或暂时断网后仍保持原值。
- 只有设备成功连接目标路由器并获取 IP 地址后，才能将其设置为 `true`。
- 恢复出厂设置或明确清除配网历史后，将其重置为 `false`。
- `hasProvisionedBefore` 不代表设备当前已连接 Wi-Fi；当前状态仍以 `provisioningState` 为准。

当 App 读取到 `hasProvisionedBefore: true` 时，应弹出确认提示：

```text
该设备之前已配置过 Wi-Fi，继续操作将切换设备连接的 Wi-Fi。
是否继续？
```

用户选择“继续切换”后才能进入家庭 Wi-Fi 选择和密码提交页面；用户取消则退出本次配网流程，不修改设备中已保存的配置。

`GET /api/v1/device` 可匿名调用。响应中的 `provisioningToken` 需通过
`X-Provisioning-Token` 请求头传给其余接口；设备每次启动都会生成新的令牌。

#### 扫描附近 Wi-Fi

```http
GET /api/v1/wifi/scan
```

返回内容包括：

- SSID
- RSSI 信号强度
- 加密类型
- 信道

#### 提交 Wi-Fi 配置

```http
POST /api/v1/wifi/config
Content-Type: application/json
X-Provisioning-Token: <token>
```

请求示例：

```json
{
  "ssid": "HomeWiFi",
  "password": "example-password"
}
```

#### 查询连接状态

```http
GET /api/v1/wifi/status
```

#### 清除 Wi-Fi 配置

```http
POST /api/v1/wifi/reset
```

#### 重启设备

```http
POST /api/v1/device/reboot
```

### 4.2 协议规范

还需要统一定义：

- JSON 数据结构。
- HTTP 状态码。
- 业务错误码。
- 请求超时时间。
- 重试次数和重试间隔。
- API 版本规则。
- Android 中文错误提示映射。
- SSID 包含中文、空格及特殊字符时的编码方式。
- Wi-Fi 密码最大长度与输入校验规则。

## 5. ESP32 固件开发清单

### 5.1 基础工程

- 建立 Arduino 或 ESP-IDF 工程。
- 配置目标为 ESP32-S3。
- 配置 16MB Flash。
- 配置 8MB OPI PSRAM。
- 建立串口日志系统。
- 规划 Flash 分区。
- 配置 NVS 持久化存储。
- 定义固件版本号。

### 5.2 Wi-Fi 状态管理

固件需要覆盖以下状态：

- 未配网。
- SoftAP 配网中。
- 已收到配网信息。
- 正在连接目标路由器。
- 连接成功。
- 连接失败。
- Wi-Fi 断开后自动重连。
- 恢复出厂设置。

建议使用明确的状态机管理状态切换，避免多个异步事件互相干扰。

### 5.3 配网功能

- 创建 SoftAP。
- 根据设备信息生成唯一热点名称。
- 配网热点使用开放网络，依靠本次启动随机 token 保护修改接口。
- 设置固定局域网地址，例如 `192.168.4.1`。
- 扫描附近的 2.4GHz Wi-Fi。
- 启动 HTTP API 服务。
- 接收并验证 SSID 和密码。
- 将凭据保存到 NVS。
- 持久化记录设备是否曾经成功完成配网。
- 连接目标路由器。
- 返回连接结果和获取到的 IP 地址。
- 配网完成后关闭 SoftAP。

### 5.4 容错功能

- Wi-Fi 连接超时。
- Wi-Fi 密码错误。
- 找不到目标 SSID。
- 路由器关闭或重启。
- DHCP 获取地址失败。
- ESP32 在配网过程中意外重启。
- 保存的数据损坏或格式不正确。
- 连续失败后重新进入配网模式。
- 长按 BOOT 键5秒进入配网模式，不清除旧 Wi-Fi 配置。
- 配网模式中短按 BOOT 键退出配网并关闭热点。
- 配网模式中 RGB 指示灯使用蓝色呼吸效果，退出后熄灭。
- 防止启动失败造成无限重启。

### 5.5 安全设计

- SoftAP 为开放网络；修改接口必须校验本次启动随机 token。
- 配网接口增加一次性 token。
- 限制 HTTP 请求体长度。
- 验证输入字段、类型和长度。
- 配网完成后立即关闭配网接口。
- 日志中禁止打印 Wi-Fi 密码。
- 必要时增加应用层加密。
- 评估是否启用 NVS 加密。
- 量产前评估 Secure Boot 和 Flash Encryption。

## 6. Android App 开发清单

### 6.1 工程基础

- 确定使用 Kotlin。
- 确定使用 Jetpack Compose 或传统 XML UI。
- 配置 Android 最低支持版本。
- 建立网络请求模块。
- 建立状态管理模块。
- 建立日志和错误收集模块。
- 规划多语言支持。

### 6.2 Android 权限与系统适配

需要针对不同 Android 版本处理：

- Wi-Fi 状态权限。
- 附近 Wi-Fi 设备权限。
- 定位权限。
- 系统定位服务是否开启。
- Android 10 之后的 Wi-Fi 连接限制。
- 使用 `WifiNetworkSpecifier` 连接设备热点。
- 系统要求用户确认连接。
- 无互联网热点的系统提示。
- 蜂窝网络与 ESP32 局域网请求冲突。
- 将 HTTP 请求绑定到指定 Wi-Fi 网络。
- 配网完成后恢复手机原有网络。

### 6.3 页面清单

1. 配网说明页。
2. 权限申请页。
3. 设备发现或热点选择页。
4. 连接 ESP32 热点页。
5. 家庭 Wi-Fi 列表页。
6. Wi-Fi 密码输入页。
7. 配网进度页。
8. 配网成功页。
9. 配网失败与问题排查页。
10. 已配设备管理页（如有需要）。

### 6.4 App 核心功能

- 识别符合命名规则的 ESP32 热点。
- 请求系统连接 ESP32 热点。
- 将网络请求绑定到 ESP32 Wi-Fi 网络。
- 获取设备信息。
- 根据 `hasProvisionedBefore` 判断是否提示用户切换 Wi-Fi。
- 获取附近路由器列表。
- 标识或过滤不支持的 5GHz 网络。
- 输入、显示和隐藏 Wi-Fi 密码。
- 提交配网信息。
- 轮询或监听配网状态。
- 处理超时和重试。
- 配网完成后恢复手机原网络。
- 提供重新配网和恢复出厂引导。

## 7. UI 与交互注意事项

- 明确提示设备仅支持 2.4GHz Wi-Fi。
- 提醒用户不要在配网过程中关闭 ESP32 热点。
- 告知用户连接无互联网热点属于正常现象。
- 配网过程中防止重复提交。
- 清晰展示当前阶段：
  - 正在连接设备。
  - 正在发送 Wi-Fi 信息。
  - 设备正在连接路由器。
  - 正在确认连接结果。
- 错误信息需要区分：
  - 手机未连接设备热点。
  - ESP32 无响应。
  - Wi-Fi 密码错误。
  - 未找到目标网络。
  - 信号太弱。
  - DHCP 获取地址失败。
  - 配网操作超时。

## 8. 联调测试清单

### 8.1 网络场景

- 普通 2.4GHz WPA2 网络。
- 2.4GHz 和 5GHz 使用相同 SSID。
- 隐藏 SSID。
- 中文 SSID。
- SSID 或密码包含特殊字符。
- 无密码网络。
- 密码错误。
- Wi-Fi 信号较弱。
- 路由器在配网过程中重启。
- 路由器无法分配 DHCP 地址。
- 手机同时开启移动数据。
- 手机开启 VPN 或网络代理。

### 8.2 Android 设备兼容性

- 不同 Android 系统版本。
- 小米。
- 华为。
- OPPO。
- vivo。
- 三星。
- Google Pixel。
- 系统拒绝连接无互联网热点。
- 用户拒绝权限。
- App 进入后台后恢复。
- 配网时手机锁屏。
- App 被系统终止后重新进入。

### 8.3 ESP32 测试

- 首次启动。
- 已保存正确的 Wi-Fi 配置。
- 已保存错误的 Wi-Fi 配置。
- 配网过程中掉电重启。
- 连续连接失败。
- 长按按键清除配置。
- NVS 数据异常。
- 多次重复配网。
- 多台 ESP32 热点同时存在。

## 9. 推荐开发顺序

1. 确定完整配网流程。
2. 确定接口协议和业务错误码。
3. 开发 ESP32 SoftAP、Wi-Fi 扫描和 HTTP API。
4. 使用电脑或手机浏览器验证 ESP32 接口。
5. 开发 Android 权限与热点连接模块。
6. 开发 Android 配网页面。
7. 完成 Android App 与 ESP32 联调。
8. 添加断线、超时、重试和恢复出厂机制。
9. 完成安全加固。
10. 覆盖不同 Android 品牌和路由器测试。
11. 根据产品需求接入账号、设备绑定和云端服务。

## 10. 项目交付物

- ESP32 固件源码。
- ESP32 可直接烧录的固件文件。
- Android App 源码。
- Android 测试 APK。
- HTTP 配网协议文档。
- 业务错误码文档。
- ESP32 烧录说明。
- Android 安装和配网说明。
- 恢复出厂设置说明。
- 测试用例和测试记录。

## 11. 开发前的关键决策

开发前需要重点确定安全等级：

### 简单测试方案

- ESP32 使用开放热点或固定密码热点。
- App 通过普通 HTTP 发送 Wi-Fi 信息。
- 适合快速验证和内部测试。

### 正式产品方案

- 使用开放热点，所有修改类接口必须校验本次启动生成的随机 token。
- 使用一次性配网 token。
- 对 Wi-Fi 凭据增加应用层加密。
- 配网成功后立即关闭热点和配网接口。
- 根据产品安全要求启用 NVS 加密、Secure Boot 和 Flash Encryption。

正式产品建议采用第二种方案。
